# Vectis Mail Server — Traefik static configuration
# Generated by config engine — do not edit manually

api:
  dashboard: false

ping: {}

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"

{{ if eq .TLS.Provider "letsencrypt" }}
certificatesResolvers:
  letsencrypt:
    acme:
      email: {{ .TLS.Email }}
      # Stored outside /etc/traefik because that mount is read-only —
      # Docker cannot create the /etc/traefik/acme mountpoint inside a
      # read-only overlay, and traefik needs to write acme.json as certs
      # are issued/renewed.
      storage: /var/traefik/acme/acme.json
      httpChallenge:
        entryPoint: web
{{ end }}

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: {{ .Logging.Level | upper }}
  format: json
