# Vectis per-domain allow list (email scope) — generated, do not edit
{{ range .SpamListEntries }}{{ if and (eq .Kind "allow") (eq .Scope "email") }}{{ .DomainName }}:{{ .Pattern }}
{{ end }}{{ end }}